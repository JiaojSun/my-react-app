import * as React from 'react';

export default class Catalog extends React.Component {
    render() {
        return (
            <div
                style={{
                    textAlign: 'center',
                    lineHeight: '100px',
                    fontSize: '30px',
                    color: '#cccccc'
                }}
            >
                Catalog
            </div>
        );
    }
}
